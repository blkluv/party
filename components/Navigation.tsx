import React from 'react'

const defaultRotes = [];
const loggedInRoutes = [];
const loggedOutRoutes = [];
const adminRoutes = [];
const drawerRoutes = [];

export default function Navigation() {
    return (
        <div>

        </div>
    )
}
