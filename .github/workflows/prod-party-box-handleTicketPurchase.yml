name: Prod/API/party-box-handleTicketPurchase

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-handleTicketPurchase/**"
      - ".github/workflows/prod-party-box-handleTicketPurchase.yml"
      - ".github/workflows/cd-lambda-prod.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-handleTicketPurchase
    secrets: inherit
