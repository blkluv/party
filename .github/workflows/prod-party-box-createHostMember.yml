name: Prod/API/party-box-createHostMember

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-createHostMember/**"
      - ".github/workflows/prod-party-box-createHostMember.yml"
      - ".github/workflows/cd-lambda-prod.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-createHostMember
    secrets: inherit
