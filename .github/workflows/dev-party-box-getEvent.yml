name: Dev/API/party-box-getEvent

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getEvent/**"
      - ".github/workflows/dev-party-box-getEvent.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"
      - "compose.yml"
      - "packages/common/**"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-getEvent
    secrets: inherit
