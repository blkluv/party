name: Prod/API/party-box-getUserHosts

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-getUserHosts/**"
      - ".github/workflows/prod-party-box-getUserHosts.yml"
      - ".github/workflows/cd-lambda-prod.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-getUserHosts
    secrets: inherit
