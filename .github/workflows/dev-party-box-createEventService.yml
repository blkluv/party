name: Dev/API/party-box-createEventService

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-createEventService/**"
      - ".github/workflows/dev-party-box-createEventService.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"
      - "compose.yml"
      - "packages/common/**"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-createEventService
    secrets: inherit
