name: Dev/API/party-box-updateEvent

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-updateEvent/**"
      - ".github/workflows/dev-party-box-updateEvent.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-updateEvent
    secrets: inherit
