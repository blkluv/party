name: Dev/API/party-box-uploadServiceMedia

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-uploadServiceMedia/**"
      - ".github/workflows/dev-party-box-uploadServiceMedia.yml"
      - ".github/workflows/cd-lambda-dev.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-uploadServiceMedia
    secrets: inherit
