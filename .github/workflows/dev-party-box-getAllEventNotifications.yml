name: Dev/API/party-box-getAllEventNotifications

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getAllEventNotifications/**"
      - ".github/workflows/dev-party-box-getAllEventNotifications.yml"
      - ".github/workflows/cd-lambda-dev.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-getAllEventNotifications
    secrets: inherit
