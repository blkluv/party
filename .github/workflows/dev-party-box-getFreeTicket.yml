name: Dev/API/party-box-getFreeTicket

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getFreeTicket/**"
      - ".github/workflows/dev-party-box-getFreeTicket.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"
      - "compose.yml"
      - "packages/common/**"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-getFreeTicket
    secrets: inherit
