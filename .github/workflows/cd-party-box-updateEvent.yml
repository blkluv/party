name: CD/party-box-updateEvent

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-updateEvent/**"
      - ".github/workflows/cd-party-box-updateEvent.yml"
      - ".github/workflows/cd-lambda-default.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-default.yml
    with:
      function-name: party-box-updateEvent
    secrets: inherit
