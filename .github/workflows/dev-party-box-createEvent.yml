name: Dev/API/party-box-createEvent

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-createEvent/**"
      - ".github/workflows/dev-party-box-createEvent.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-createEvent
    secrets: inherit
