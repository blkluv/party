name: CD/party-box-uploadEventMedia

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-uploadEventMedia/**"
      - ".github/workflows/cd-party-box-uploadEventMedia.yml"
      - ".github/workflows/cd-lambda-default.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-default.yml
    with:
      function-name: party-box-uploadEventMedia
    secrets: inherit
