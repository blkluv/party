name: Prod/API/party-box-getHostRoles

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-getHostRoles/**"
      - ".github/workflows/prod-party-box-getHostRoles.yml"
      - ".github/workflows/cd-lambda-prod.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-getHostRoles
    secrets: inherit
