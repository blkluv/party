name: Prod/API/party-box-getEvent

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-getEvent/**"
      - ".github/workflows/prod-party-box-getEvent.yml"
      - ".github/workflows/cd-lambda-prod.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-getEvent
    secrets: inherit
