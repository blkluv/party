name: Prod/API/party-box-sendEventNotifications

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-sendEventNotifications/**"
      - ".github/workflows/prod-party-box-sendEventNotifications.yml"
      - ".github/workflows/cd-lambda-prod.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-sendEventNotifications
    secrets: inherit
