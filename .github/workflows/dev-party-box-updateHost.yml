name: Dev/API/party-box-updateHost

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-updateHost/**"
      - ".github/workflows/dev-party-box-updateHost.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-updateHost
    secrets: inherit
