name: Prod/API/party-box-createHost

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-createHost/**"
      - ".github/workflows/prod-party-box-createHost.yml"
      - ".github/workflows/cd-lambda-prod.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-createHost
    secrets: inherit
