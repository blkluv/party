name: Dev/API/party-box-getTicket

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getTicket/**"
      - ".github/workflows/dev-party-box-getTicket.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-getTicket
    secrets: inherit
