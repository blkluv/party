name: Prod/API/party-box-getTicket

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-getTicket/**"
      - ".github/workflows/prod-party-box-getTicket.yml"
      - ".github/workflows/cd-lambda-prod.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-getTicket
    secrets: inherit
