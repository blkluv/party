name: Prod/API/party-box-postSignUp

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-postSignUp/**"
      - ".github/workflows/prod-party-box-postSignUp.yml"
      - ".github/workflows/cd-lambda-prod.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-postSignUp
    secrets: inherit
