name: CD/party-box-createEvent

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-createEvent/**"
      - ".github/workflows/cd-party-box-createEvent.yml"
      - ".github/workflows/cd-lambda-default.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-default.yml
    with:
      function-name: party-box-createEvent
    secrets: inherit
