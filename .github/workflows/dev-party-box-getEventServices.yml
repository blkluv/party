name: Dev/API/party-box-getEventServices

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getEventServices/**"
      - ".github/workflows/dev-party-box-getEventServices.yml"
      - ".github/workflows/cd-lambda-dev.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-getEventServices
    secrets: inherit
