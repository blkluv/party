name: Prod/API/party-box-getHostProfile

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-getHostProfile/**"
      - ".github/workflows/prod-party-box-getHostProfile.yml"
      - ".github/workflows/cd-lambda-prod.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-getHostProfile
    secrets: inherit
