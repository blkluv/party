name: CD/party-box-updateTicketUsage

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-updateTicketUsage/**"
      - ".github/workflows/cd-party-box-updateTicketUsage.yml"
      - ".github/workflows/cd-lambda-default.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-default.yml
    with:
      function-name: party-box-updateTicketUsage
    secrets: inherit
