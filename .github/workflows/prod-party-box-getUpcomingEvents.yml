name: Prod/API/party-box-getUpcomingEvents

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-getUpcomingEvents/**"
      - ".github/workflows/prod-party-box-getUpcomingEvents.yml"
      - ".github/workflows/cd-lambda-prod.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-getUpcomingEvents
    secrets: inherit
