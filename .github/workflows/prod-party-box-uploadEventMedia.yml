name: Prod/API/party-box-uploadEventMedia

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-uploadEventMedia/**"
      - ".github/workflows/prod-party-box-uploadEventMedia.yml"
      - ".github/workflows/cd-lambda-prod.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-uploadEventMedia
    secrets: inherit
