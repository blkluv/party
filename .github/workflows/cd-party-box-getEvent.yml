name: CD/party-box-getEvent

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getEvent/**"
      - ".github/workflows/cd-party-box-getEvent.yml"
      - ".github/workflows/cd-lambda-default.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-default.yml
    with:
      function-name: party-box-getEvent
    secrets: inherit
