name: Dev/API/party-box-getUpcomingEvents

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getUpcomingEvents/**"
      - ".github/workflows/dev-party-box-getUpcomingEvents.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-getUpcomingEvents
    secrets: inherit
