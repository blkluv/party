name: CD/party-box-getTicket

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getTicket/**"
      - ".github/workflows/cd-party-box-getTicket.yml"
      - ".github/workflows/cd-lambda-default.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-default.yml
    with:
      function-name: party-box-getTicket
    secrets: inherit
