name: Prod/API/party-box-deleteHost

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-deleteHost/**"
      - ".github/workflows/prod-party-box-deleteHost.yml"
      - ".github/workflows/cd-lambda-prod.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-deleteHost
    secrets: inherit
