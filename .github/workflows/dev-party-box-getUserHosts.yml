name: Dev/API/party-box-getUserHosts

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getUserHosts/**"
      - ".github/workflows/dev-party-box-getUserHosts.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"
      - "compose.yml"
      - "packages/common/**"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-getUserHosts
    secrets: inherit
