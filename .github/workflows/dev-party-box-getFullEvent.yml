name: Dev/API/party-box-getFullEvent

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getFullEvent/**"
      - ".github/workflows/dev-party-box-getFullEvent.yml"
      - ".github/workflows/cd-lambda-dev.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-getFullEvent
    secrets: inherit
