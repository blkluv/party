name: Dev/API/party-box-handleTicketPurchase

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-handleTicketPurchase/**"
      - ".github/workflows/dev-party-box-handleTicketPurchase.yml"
      - ".github/workflows/cd-lambda-dev.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-handleTicketPurchase
    secrets: inherit
