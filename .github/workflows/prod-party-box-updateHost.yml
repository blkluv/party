name: Prod/API/party-box-updateHost

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-updateHost/**"
      - ".github/workflows/prod-party-box-updateHost.yml"
      - ".github/workflows/cd-lambda-prod.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-updateHost
    secrets: inherit
