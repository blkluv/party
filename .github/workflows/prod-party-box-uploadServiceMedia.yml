name: Prod/API/party-box-uploadServiceMedia

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-uploadServiceMedia/**"
      - ".github/workflows/prod-party-box-uploadServiceMedia.yml"
      - ".github/workflows/cd-lambda-prod.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-uploadServiceMedia
    secrets: inherit
