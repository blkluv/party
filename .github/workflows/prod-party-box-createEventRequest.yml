name: Prod/API/party-box-createEventRequest

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-createEventRequest/**"
      - ".github/workflows/prod-party-box-createEventRequest.yml"
      - ".github/workflows/cd-lambda-prod.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-createEventRequest
    secrets: inherit
