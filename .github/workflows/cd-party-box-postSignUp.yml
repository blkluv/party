name: CD/party-box-postSignUp

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-postSignUp/**"
      - ".github/workflows/cd-party-box-postSignUp.yml"
      - ".github/workflows/cd-lambda-default.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-default.yml
    with:
      function-name: party-box-postSignUp
    secrets: inherit
