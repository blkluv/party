name: Prod/API/party-box-getFullEvent

on:
  push:
    branches:
      - main
    paths:
      - "api/party-box-getFullEvent/**"
      - ".github/workflows/prod-party-box-getFullEvent.yml"
      - ".github/workflows/cd-lambda-prod.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-prod.yml
    with:
      function-name: party-box-getFullEvent
    secrets: inherit
