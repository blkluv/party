name: Dev/API/party-box-sendEventNotifications

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-sendEventNotifications/**"
      - ".github/workflows/dev-party-box-sendEventNotifications.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"
      - "compose.yml"
      - "packages/common/**"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-sendEventNotifications
    secrets: inherit
