name: CD/party-box-getUpcomingEvents

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-getUpcomingEvents/**"
      - ".github/workflows/cd-party-box-getUpcomingEvents.yml"
      - ".github/workflows/cd-lambda-default.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-default.yml
    with:
      function-name: party-box-getUpcomingEvents
    secrets: inherit
