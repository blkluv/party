name: Dev/API/party-box-createEventRequest

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-createEventRequest/**"
      - ".github/workflows/dev-party-box-createEventRequest.yml"
      - ".github/workflows/cd-lambda-dev.yml"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-createEventRequest
    secrets: inherit
