name: Dev/API/party-box-updateTicketUsage

on:
  push:
    branches:
      - dev
    paths:
      - "api/party-box-updateTicketUsage/**"
      - ".github/workflows/dev-party-box-updateTicketUsage.yml"
      - ".github/workflows/cd-lambda-dev.yml"
      - "docker/api/Dockerfile"

jobs:
  deploy:
    name: Deploy Lambda
    uses: ./.github/workflows/cd-lambda-dev.yml
    with:
      function-name: party-box-updateTicketUsage
    secrets: inherit
